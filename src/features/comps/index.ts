// The comps page. Only the page's own entry point is exported: the criteria
// state, the hooks, the panel and the card are this folder's own, on the barrel
// rule the `features/shared` header states.
export { CompsHome } from "./components/comps-home";
