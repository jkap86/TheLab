// The visit log page. Only the root client component leaves this folder; the
// hook, the table and the two pure helpers are its own parts, on the barrel
// rule the other feature folders keep.

export { LogsHome } from "./components/logs-home";
