// The visit log page. Only the two root client components leave this folder —
// the table for a live session, the sign-in for none; the hook, the table and
// the two pure helpers are its own parts, on the barrel rule the other feature
// folders keep.

export { LogsHome } from "./components/logs-home";
export { LogsLogin } from "./components/logs-login";
