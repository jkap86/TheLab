// The lineup checker. Only the page's root client component leaves this folder;
// the hook, the card and the helpers are its own parts, on the barrel rule the
// manager and tools folders keep.

export { LineupCheckerHome } from "./components/lineup-checker-home";
