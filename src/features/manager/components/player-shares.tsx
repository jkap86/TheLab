// The player-shares list. It moved to `features/shared` with the sheet that draws
// it over a page — the lineup checker opens the same browse — and is re-exported
// here for the Players tab.
export { PlayerShares } from "@/features/shared/ui/shares/player-shares";
