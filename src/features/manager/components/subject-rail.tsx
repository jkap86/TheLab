// The page's filter row — what these leagues are, who is in them, what survives.
// It moved to `features/shared` when the lineup checker grew the same row, and is
// re-exported here for `leagues-view-layout`.
export { SubjectRail } from "@/features/shared/ui/subject-rail";
