// The list's heading billet and the subject rail above it. They moved to
// `features/shared` when the lineup checker started drawing the same pair;
// re-exported here for this feature's three tab views.
export { ColumnsBar } from "@/features/shared/ui/columns-bar";
