// The trades board. Only the page's own entry point is exported: everything
// else — the filter vocabulary, the query builder, the hooks, the card — is
// this folder's own, on the barrel rule the `features/shared` header states.
export { TradesHome } from "./components/trades-home";
