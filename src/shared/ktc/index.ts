// KeepTradeCut, of which only the superflex predicate has arrived — see
// `roster.ts` for what the rest is and when it comes. Import from here;
// test-covered modules inside `shared/` read `roster.ts` relatively instead,
// the mechanism `projections/slots.ts` documents.

export { isSuperflexLineup, QB_ELIGIBLE_STARTING_SLOTS } from "./roster";
