// Superseded by `@/shared/sleeper`, which is where TheLabX keeps this and where
// the CDN base it builds on now lives. Nothing imports it any more; safe to
// delete.

export { sleeperAvatarUrl, SLEEPER_CDN_BASE } from "@/shared/sleeper";
